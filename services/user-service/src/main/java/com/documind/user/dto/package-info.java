/**
 * Contains Data Transfer Object (DTO) classes for user-related operations.
 *
 * <p>These classes represent the data structure for API requests and responses, providing a clear
 * contract between the client and server. They include validation constraints and are designed to
 * exclude sensitive information when returned to clients.
 */
package com.documind.user.dto;
