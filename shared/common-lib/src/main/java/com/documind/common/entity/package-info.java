/**
 * Package containing the core entity classes and base definitions for the application's data model.
 *
 * <p>This package includes the {@link com.documind.common.entity.BaseEntity} which serves as the
 * foundation for all persistent entities in the system, providing common fields and behavior such
 * as auditing, versioning, and soft delete capabilities.
 *
 * <p>All entity classes in the application should extend the base entity to maintain consistency in
 * the data model and leverage the common functionality provided.
 *
 * @see com.documind.common.entity.BaseEntity
 */
package com.documind.common.entity;
