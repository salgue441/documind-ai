/**
 * Package containing application configuration classes.
 *
 * <p>This package includes configuration for:
 *
 * <ul>
 *   <li>JPA auditing ({@link com.documind.common.config.AuditConfig})
 *   <li>API documentation ({@link com.documind.common.config.OpenApiConfig})
 *   <li>Web/CORS settings ({@link com.documind.common.config.WebConfig})
 *   <li>General JPA configuration ({@link com.documind.common.config.JpaConfig})
 * </ul>
 *
 * <p>All configurations are Spring {@code @Configuration} classes that are automatically picked up
 * during component scanning.
 */
package com.documind.common.config;
